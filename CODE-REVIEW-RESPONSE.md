# Response to CODE-REVIEW.md

Each point evaluated, then fixed or declined with a reason. Verification after the changes: 130 tests (was 122), tsc clean, oxlint clean with `react/exhaustive-deps` on, prettier clean, build ok, 100% statement/line/function coverage, 97.9% branches.

## Findings

| #                                             | Verdict  | What changed                                                                                                                                                                                                                                                                                                                             |
| --------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 Backdrop click on dialog padding            | Fixed    | `dialog { padding: 0 }`, padding moved to `.dialog-body`. Comment in `EventDetailModal.tsx` explains why.                                                                                                                                                                                                                                |
| 2 Double-click Delete sends two requests      | Fixed    | `useEvents` tracks `pendingIds`; `EventList` disables that row's button while in flight. Test: `3-delete > disables the button while the delete is in flight`.                                                                                                                                                                           |
| 3 Overlapping reloads, last response wins     | Fixed    | `reload` carries a monotonic token in a ref; a response that is no longer the latest is dropped. `getOne` effects in the modal and edit page set a `stale` flag in cleanup. Test: `3-delete > a stale refetch response cannot overwrite the newer list` (verified to fail without the guard).                                            |
| 4 Malformed record hangs on Loading           | Fixed    | `reload` is `.then(...).catch(...)` so a throw in the success path reaches `setError`; `sortByCompany` coerces with `String()`. Tests: `4-errors > wrong shape` and `not an array`, `sort.test > number or object`. No response-shape validator added: the coercion plus catch makes bad data fail safe, which is enough for this scope. |
| 5 mutate returns true when the refetch failed | Fixed    | `reload` resolves a boolean and `mutate` returns it, so the form keeps its values. Test: `4-errors > keeps the typed values when the POST succeeds but the refetch fails`.                                                                                                                                                               |
| 6 404 on delete leaves a stale row            | Fixed    | `mutate` refetches before setting the error when the status is 404. The existing 404 test now asserts the row is gone.                                                                                                                                                                                                                   |
| 7 Error state not reset on id change          | Declined | The route table guarantees the modal and edit page unmount between ids, so the state is fresh. Resetting inside the effect would trip the set-state-in-effect lint rule for a path that cannot occur. The `stale` flag from finding 3 covers the response-ordering half.                                                                 |
| 8 Content-Type on GET/DELETE forces preflight | Fixed    | `request()` sends the header only with a body. The unused `init.headers` merge is gone; `request` takes `(url, method, body)`. Test updated to assert per method.                                                                                                                                                                        |
| 9 204 reported as an error                    | Fixed    | `request()` returns `undefined` on 204. Test added.                                                                                                                                                                                                                                                                                      |
| 10 Untrusted color in inline style            | Fixed    | Swatch renders only when `isNamedColor` passes.                                                                                                                                                                                                                                                                                          |

## Code reuse and structure

| Point                                                       | Verdict           | What changed                                                                                                                                                                                                        |
| ----------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Trimming in three places                                    | Fixed             | `normalizeValues` (renamed from `trimValues`) runs once in `EventForm.handleSubmit`; both `validateEvent` and `onSubmit` receive normalized values. `validateEvent`, `buildNewEvent`, `applyEdits` no longer trim.  |
| Field list declared repeatedly                              | Fixed             | `EVENT_FIELDS` exported from `types.ts`; `EventFormValues` is derived from it; `validate.ts` and `EventForm.tsx` iterate it. The object literals in `events.ts` stay because the type checks them for completeness. |
| `initial === EMPTY` identity check                          | Fixed             | Replaced by an explicit `resetOnSuccess` prop; `EventsPage` passes it.                                                                                                                                              |
| Hard-coded element ids                                      | Fixed             | `useId()` prefixes input, error and datalist ids.                                                                                                                                                                   |
| Inconsistent id types                                       | Fixed             | `EventId = number \| string` in `types.ts`; all five API functions accept it.                                                                                                                                       |
| `setSubmitting(false)` after unmount                        | Declined          | React 18+ does not warn and there is no leak; moving the state to the parent adds a prop for no user-visible change.                                                                                                |
| UTC date/time, createdOn format                             | Fixed (date/time) | `buildNewEvent` uses local date (`en-CA`) and local time (`en-GB`, 24h). `createdOn` stays ISO 8601; the seed's `+06:00` with a space is faker output nobody parses.                                                |
| Label map instead of CSS capitalize                         | Declined          | Four labels; the CSS rule is one line and the tests match the DOM text.                                                                                                                                             |
| `aria-labelledby` points at a missing element while loading | Fixed             | `aria-labelledby` only once the heading exists, `aria-label="Event details"` before.                                                                                                                                |
| Focus not returned to the triggering link                   | Declined          | Outside the assignment's scope; would need the list to remember the clicked row across a route change.                                                                                                              |

## Tests

| Point                                             | Verdict | What changed                                                                                                                                   |
| ------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| No concurrent-request tests                       | Fixed   | Double-click delete; stale refetch overtaken by a newer one.                                                                                   |
| No malformed-record test                          | Fixed   | Non-string company; non-array list response.                                                                                                   |
| `captureRequests` listener leak                   | Fixed   | `server.events.removeAllListeners()` in `afterEach`.                                                                                           |
| Image test renders twice and indexes `dialogs[1]` | Fixed   | Split into two tests.                                                                                                                          |
| `dialog.close()` outside `act`                    | Fixed   | Wrapped; the warning is gone from the run output.                                                                                              |
| 404 tests stop at the banner                      | Fixed   | Delete 404 test asserts the row is gone. Update 404 test unchanged: the page stays on the form with the error, which is the intended behavior. |
| Sort test only covers `undefined`                 | Fixed   | Number and object cases added.                                                                                                                 |
| Content-Type test codifies finding 8              | Fixed   | Asserts header per method.                                                                                                                     |
| Live-test config with no live test                | Fixed   | Exclude and comment removed.                                                                                                                   |
| 148-color check is circular                       | Fixed   | Spot-checks six known names and four known non-names, keeps the size check.                                                                    |

## Housekeeping

| Point                        | Verdict         | What changed                                                                                     |
| ---------------------------- | --------------- | ------------------------------------------------------------------------------------------------ |
| Not a git repository         | Fixed           | `git init` and an initial commit on the Mac copy.                                                |
| `coverage/` not ignored      | Fixed           | Added to `.gitignore`.                                                                           |
| `.DS_Store`                  | Already ignored | Covered once the repo exists.                                                                    |
| prettier not a devDependency | Fixed           | `prettier@3` in devDependencies, `format` and `format:check` scripts, `.prettierignore`.         |
| No Node version pin          | Fixed           | `.nvmrc` = 22.                                                                                   |
| `exhaustive-deps` off        | Fixed           | `react/exhaustive-deps: error` in `.oxlintrc.json`; verified it fires on a deliberate violation. |
| Requirement table duplicated | Fixed           | Removed from `docs/architecture.md`; README's table gained a Diagram column.                     |
